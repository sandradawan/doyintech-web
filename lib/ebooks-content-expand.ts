/**
 * Placeholder — chapter expansion previously used fs JSON loads.
 * Kept so any stale import does not break the build.
 */
export const FULL_CHAPTERS: Record<
  string,
  { title: string; body: string; imageCaption?: string }[]
> = {};

export const FULL_CHAPTERS_MORE: Record<
  string,
  { title: string; body: string; imageCaption?: string }[]
> = {};

export const FULL_CHAPTERS_LIFE: Record<
  string,
  { title: string; body: string; imageCaption?: string }[]
> = {};
